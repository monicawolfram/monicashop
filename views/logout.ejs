<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Logged Out</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #dbeafe, #c7d2fe); /* soft blue gradient */
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .logout-container {
      background: #ffffff;
      border-radius: 16px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.12);
      text-align: center;
      padding: 50px 35px;
      max-width: 450px;
      width: 100%;
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .logout-container:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 30px rgba(0,0,0,0.18);
    }
    .logout-container i {
      font-size: 5rem;
      color: #3b82f6; /* blue icon */
      margin-bottom: 20px;
    }
    .logout-container h2 {
      color: #1e293b; /* dark slate */
      margin-bottom: 12px;
      font-weight: 600;
    }
    .logout-container p {
      color: #475569; /* grayish text */
      margin-bottom: 25px;
      font-size: 1rem;
    }
    .btn-login {
      font-size: 1rem;
      padding: 12px 30px;
      border-radius: 8px;
      background: #3b82f6;
      border: none;
    }
    .btn-login:hover {
      background: #2563eb;
    }
    .countdown {
      color: #3b82f6; /* matches icon color */
      font-weight: bold;
    }
  </style>
</head>
<body>

<div class="logout-container">
  <i class="fas fa-sign-out-alt"></i>
  <h2>Logged Out</h2>
  <p>You have successfully logged out of your agent account.</p>
  <p>Redirecting to login in <span class="countdown" id="countdown">5</span> seconds...</p>
  <a href="/agent/login" class="btn btn-login"><i class="fas fa-arrow-left"></i> Go to Login</a>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script>
  let timeLeft = 5;
  const countdownEl = document.getElementById('countdown');

  const timer = setInterval(() => {
    timeLeft--;
    countdownEl.textContent = timeLeft;
    if (timeLeft <= 0) {
      clearInterval(timer);
      window.location.href = '/agent/login';
    }
  }, 1000);
</script>

</body>
</html>
